function apresentar(nome) {
    return `meu nome é ${nome}`;
}

// Arrow Function
const apresentarArrow = nome => `meu nome é ${nome}`;
const soma = (num1, num2) => num1 + num2;
/* 
Qual a diferença entre Arrow Function e função tradicional?
A Arrow Function é sempre uma função anônima, enquanto a função tradicional pode ser anônima ou não.
*/