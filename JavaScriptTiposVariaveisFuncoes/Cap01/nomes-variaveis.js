// Nomes de Variáveis em JavaScript
const minhaVariavel = 1;
const MinhaVariavel = "texto";
const minhavar = "3";
const MINHAVARIAVEL = 2;

console.log(minhaVariavel, MinhaVariavel, minhavar, MINHAVARIAVEL)

// camelCase: é o padrão adotado pelo JavaScript
// snake_case: é o padrão adotado pelo Python
// kebab-case: é o padrão adotado pelo CSS
// PascalCase: é o padrão adotado pelo C# quando declaramos classes